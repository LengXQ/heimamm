import Vue from 'vue';
// 导入 路由
import VueRouter from 'vue-router';
//注册 vue-router
Vue.use(VueRouter);
// 实例化 
const router = new VueRouter({
    routes: [{
        path: '/',
        component: 
    }, ]
});

// 暴露出去
export default router